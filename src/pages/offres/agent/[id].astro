---
import Layout from "../../../layouts/Layout.astro";
import { getOneAgent } from "../../../js/backend.mjs";
import { getMaisonByAgent } from "../../../js/backend.mjs";
const id = Astro.params;
const agent = await getOneAgent(id);
const maisons = await getMaisonByAgent(id);
console.log(agent);
---
<Layout title = > {agent.nom} {agent.prenom}</h2>
    <ul>
        <li>{agent.email}</li> 
    </ul>
</Layout>