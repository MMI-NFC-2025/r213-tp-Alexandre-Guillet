---
const { offre } = Astro.props;
---

<div class="card bg-violet-400 text-white py-12 px-8 text-center mt-auto">
    <img src={offre.imgUrl} alt={offre.nomMaison} />

    <h2>{offre.nomMaison}</h2>
    <p>{offre.adresse}</p>

    <ul>
        <li>Chambres : {offre.nbChambres}</li>
        <li>Salles de bain : {offre.nbSdb}</li>
        <li>Surface : {offre.surface} m²</li>
        <li>Prix : {offre.prix} €</li>
    </ul>

    {offre.favori && <span class="favori bg-blend-color-burn text-yellow-400">★ Favori</span>}
</div>

<style>
    .card {
        border: 1px solid #ddd;
        padding: 1rem;
        border-radius: 8px;
        max-width: 260px;
    }
    img {
        width: 100%;
        border-radius: 6px;
    }
    .favori {
        color: rgb(174, 0, 255);
        font-weight: bold;
    }
</style>